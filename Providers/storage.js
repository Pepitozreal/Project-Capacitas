import {AsyncStorage} from 'react-native';

const Storage = {
    saveImage: (image) => {
        // Code to save image
    },
    loadImage: (key) => {
        // Code to load image
    },
    loadAllImages: () => {
        // Loads all images from storage
    }
}

export default Storage;